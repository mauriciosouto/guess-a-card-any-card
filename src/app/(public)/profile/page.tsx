import { RouteShell } from "@/components/layout/route-shell";

export default function ProfilePage() {
  return (
    <RouteShell
      title="Your sigil"
      description="Face, motto, and tally of readings — awakens when your oath binds via Supabase."
    />
  );
}
