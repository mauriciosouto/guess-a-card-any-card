/**
 * Subtle fixed atmospheric layer — pairs with `.magical-app-bg` and global `.particle-layer` styles.
 */
export function ParticleField() {
  return <div className="particle-layer" aria-hidden />;
}
